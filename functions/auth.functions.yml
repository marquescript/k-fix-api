register:
  handler: src/infra/handlers/auth/register.handler
  events:
    - httpApi:
        path: /register
        method: post

sign-in:
  handler: src/infra/handlers/auth/sign-in.handler
  events:
    - httpApi:
        path: /sign-in
        method: post